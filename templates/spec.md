# Spec {NNN}: {功能名称}

**状态**: Draft | In Review | Approved
**最后更新**: {YYYY-MM-DD}
**需求来源**: PRD | 简要描述 | 口头需求
**版本归属**: {vX.Y.Z | 无（独立变更）}

---

## 1. 背景与目标

### 用户故事
As a {角色},
I want to {行为},
So that {价值}.

### 目标用户
{描述目标用户群体}

### 触发原因
{一句话说明为什么现在启动这个 spec}

### 非目标
- {明确不做的事情，避免范围蔓延}

---

## 1.1 实现假设（可选，假设式澄清时填写）

基于对以下文件的分析：
- `{path/to/file}` — {现有模块职责}

AI 假设：
1. {假设内容}

用户纠正：
- {对错误假设的纠正，无纠正则删除本节}

---

## 2. 功能描述

{按逻辑分组描述功能细节，可用子标题拆分}

---

## 3. 参数/配置

| 参数 | 可选值 | 默认值 | 说明 |
|------|--------|--------|------|
| {参数名} | {可选值} | {默认值} | {用途说明} |

---

## 4. 验收标准（EARS 格式）

### 4.1 {功能分组名}
- When [{触发条件}], the system shall [{预期行为}].
- While [{持续条件}], the system shall [{持续行为}].

### 4.N 视觉还原（涉及 UI 还原时必填，详见 sections/mobile.md §5.9）
- When [{进入 XX 屏}], the system shall [{可像素级核对的结构/尺寸/资产条款，允许 ±2pt 误差}].

### 4.N 非功能要求
- The system shall [{安全/性能/可靠性要求}].
